class Town extends Ground {
    constructor(img, x, y, width, height) {
        super(img, x);
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
    }
}